#ifndef SHELL_STDLIB_H
#define SHELL_STDLIB_H

/**
 * shell_init_stdlib – writes the embedded ROX source files (gui.ros, etc.)
 * to /lib/ros/ so they can be imported by other programs.
 */
void shell_init_stdlib(void);

#endif /* SHELL_STDLIB_H */
